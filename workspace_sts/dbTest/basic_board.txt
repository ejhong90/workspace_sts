# 기본 게시판 테이블
CREATE TABLE BASIC_BOARD (
	BOARD_NUM INT PRIMARY KEY -- NOT NULL, 중복X
	, BOARD_TITLE VARCHAR(50) NOT NULL
	, BOARD_CONTENT VARCHAR(50)
	, WRITER VARCHAR(20) NOT NULL
	, CREATE_DATE VARCHAR(20) NOT NULL -- '2023-05-10' 형태로 입력
	, READ_CNT INT DEFAULT 0
);


# DATE 년월일
# DATETIME 년월일타임

SELECT *
FROM basic_board;

SELECT BOARD_NUM
      , BOARD_TITLE
      , WRITER
      , CREATE_DATE
      , READ_CNT
FROM basic_board;

# UPDATE 문법
UPDATE basic_board
SET 
WRITER = '홍길동';
# 이렇게 실행하면 모든 작성자의 데이터가 수정됨
# 내가 원하는 데이터에 대하여 수정을 원할 경우
UPDATE basic_board
SET 
	WRITER = '홍길동'
WHERE BOARD_NUM = 1;

COMMIT;

UPDATE basic_board
SET
	WRITER = '홍길동'
	, BOARD_CONTENT = '변경내용'
WHERE BOARD_NUM = 1;

UPDATE BASIC_BOARD
SET
   BOARD_TITLE
   , BOARD_CONTENT
WHERE BOARD_NUM = 1;

SELECT *
FROM BASIC_BOARD;